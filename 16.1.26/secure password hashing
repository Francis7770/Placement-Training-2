from argon2 import PasswordHasher

ph = PasswordHasher()
hash = ph.hash("SuperSecretPassword123")
print("Hash:", hash)

try:
    ph.verify(hash, "SuperSecretPassword123")
    print("Password verified!")
except:
    print("Invalid password")
